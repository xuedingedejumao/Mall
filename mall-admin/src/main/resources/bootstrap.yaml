spring:
  application:
    name: mall-admin
  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: localhost:8848
      config:
        enabled: true
        refresh-enabled: true
    discovery:
      enabled: true
